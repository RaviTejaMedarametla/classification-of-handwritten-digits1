# Results Artifacts

This folder stores reproducible evidence artifacts generated by scripts.

Run:

```bash
python results/run_all_experiments.py
```

Artifacts are written to `artifacts/` and `results/`.
